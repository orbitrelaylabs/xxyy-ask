name: xxyy-ask

x-app-base: &app-base
  image: xxyy-ask:local
  build:
    context: .
  env_file:
    - .env
  environment:
    DATABASE_URL: ${COMPOSE_DATABASE_URL:-}
    NODE_ENV: production
    KNOWLEDGE_AUTO_REFRESH_ENABLED: ${KNOWLEDGE_AUTO_REFRESH_ENABLED:-false}
    KNOWLEDGE_AUTO_REFRESH_INCREMENTAL_DAILY_AT: ${KNOWLEDGE_AUTO_REFRESH_INCREMENTAL_DAILY_AT:-08:00}
    KNOWLEDGE_AUTO_REFRESH_RECEIPT_FILE: ${KNOWLEDGE_AUTO_REFRESH_RECEIPT_FILE:-.rag/knowledge-refresh/latest.json}
    KNOWLEDGE_AUTO_REFRESH_STALE_AFTER_MINUTES: ${KNOWLEDGE_AUTO_REFRESH_STALE_AFTER_MINUTES:-1560}
    KNOWLEDGE_AUTO_REFRESH_TIME_ZONE: ${KNOWLEDGE_AUTO_REFRESH_TIME_ZONE:-Asia/Shanghai}
    OPENAI_API_KEY: ${OPENAI_API_KEY:?Set OPENAI_API_KEY in .env}
    OPENAI_BASE_URL: ${COMPOSE_OPENAI_BASE_URL:-${OPENAI_BASE_URL}}
    OPENAI_MODEL: ${OPENAI_MODEL:?Set OPENAI_MODEL in .env}
    POSTGRES_DB: ${POSTGRES_DB:?Set POSTGRES_DB in .env}
    POSTGRES_HOST: postgres
    POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
    POSTGRES_PORT: 5432
    POSTGRES_USER: ${POSTGRES_USER:?Set POSTGRES_USER in .env}
  init: true

services:
  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:?Set POSTGRES_DB in .env}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      POSTGRES_USER: ${POSTGRES_USER:?Set POSTGRES_USER in .env}
    ports:
      - '${POSTGRES_BIND_HOST:-127.0.0.1}:${POSTGRES_PORT:-5432}:5432'
    volumes:
      - xxyy_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U "$${POSTGRES_USER}" -d "$${POSTGRES_DB}"']
      interval: 5s
      timeout: 5s
      retries: 12

  knowledge-init:
    <<: *app-base
    command: ['node', 'scripts/bootstrap-knowledge.mjs']
    depends_on:
      postgres:
        condition: service_healthy
    restart: 'no'

  api:
    <<: *app-base
    command: ['pnpm', 'run', 'api:dev']
    restart: unless-stopped
    environment:
      ANSWER_QUALITY_OBSERVABILITY_ENABLED: ${ANSWER_QUALITY_OBSERVABILITY_ENABLED:-false}
      ANSWER_QUALITY_WEB_MODE: ${ANSWER_QUALITY_WEB_MODE:-optimized}
      ANSWER_QUALITY_WEB_OPTIMIZED_PERCENTAGE: ${ANSWER_QUALITY_WEB_OPTIMIZED_PERCENTAGE:-100}
      DATABASE_URL: ${COMPOSE_DATABASE_URL:-}
      NODE_ENV: production
      KNOWLEDGE_AUTO_REFRESH_ENABLED: ${KNOWLEDGE_AUTO_REFRESH_ENABLED:-false}
      KNOWLEDGE_AUTO_REFRESH_INCREMENTAL_DAILY_AT: ${KNOWLEDGE_AUTO_REFRESH_INCREMENTAL_DAILY_AT:-08:00}
      KNOWLEDGE_AUTO_REFRESH_RECEIPT_FILE: ${KNOWLEDGE_AUTO_REFRESH_RECEIPT_FILE:-.rag/knowledge-refresh/latest.json}
      KNOWLEDGE_AUTO_REFRESH_STALE_AFTER_MINUTES: ${KNOWLEDGE_AUTO_REFRESH_STALE_AFTER_MINUTES:-1560}
      KNOWLEDGE_AUTO_REFRESH_TIME_ZONE: ${KNOWLEDGE_AUTO_REFRESH_TIME_ZONE:-Asia/Shanghai}
      OPENAI_API_KEY: ${OPENAI_API_KEY:?Set OPENAI_API_KEY in .env}
      OPENAI_BASE_URL: ${COMPOSE_OPENAI_BASE_URL:-${OPENAI_BASE_URL}}
      OPENAI_MODEL: ${OPENAI_MODEL:?Set OPENAI_MODEL in .env}
      ONCHAIN_ALLOW_INSECURE_LOCALHOST: ${ONCHAIN_ALLOW_INSECURE_LOCALHOST:-false}
      ONCHAIN_RPC_CONFIG_JSON: ${ONCHAIN_RPC_CONFIG_JSON:-}
      XXYY_SMALL_POOL_MAX_LIQUIDITY_USD: ${XXYY_SMALL_POOL_MAX_LIQUIDITY_USD:-10000}
      XXYY_SMALL_POOL_MAX_RELATIVE_LIQUIDITY_PPM: ${XXYY_SMALL_POOL_MAX_RELATIVE_LIQUIDITY_PPM:-100000}
      XXYY_CANONICAL_POOL_CONFIG_JSON: ${XXYY_CANONICAL_POOL_CONFIG_JSON:-}
      POSTGRES_DB: ${POSTGRES_DB:?Set POSTGRES_DB in .env}
      POSTGRES_HOST: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:?Set POSTGRES_USER in .env}
      PORT: 3000
      API_ENABLE_DEEP_HEALTH: ${API_ENABLE_DEEP_HEALTH:-true}
      KNOWLEDGE_ADMIN_RATE_LIMIT_MAX: ${KNOWLEDGE_ADMIN_RATE_LIMIT_MAX:-30}
      KNOWLEDGE_ADMIN_RATE_LIMIT_WINDOW_MS: ${KNOWLEDGE_ADMIN_RATE_LIMIT_WINDOW_MS:-60000}
      TRUST_PROXY: ${TRUST_PROXY:-false}
    depends_on:
      knowledge-init:
        condition: service_completed_successfully
    ports:
      - '${APP_BIND_HOST:-127.0.0.1}:${APP_PORT:-3000}:3000'
    volumes:
      - './.rag/knowledge-refresh:/app/.rag/knowledge-refresh:ro'
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "fetch('http://127.0.0.1:3000/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 10s
      timeout: 5s
      retries: 12

  telegram:
    <<: *app-base
    command: ['pnpm', 'run', 'telegram:dev']
    restart: unless-stopped
    environment:
      ANSWER_QUALITY_OBSERVABILITY_ENABLED: ${ANSWER_QUALITY_OBSERVABILITY_ENABLED:-false}
      ANSWER_QUALITY_TELEGRAM_MODE: ${ANSWER_QUALITY_TELEGRAM_MODE:-optimized}
      ANSWER_QUALITY_TELEGRAM_OPTIMIZED_PERCENTAGE: ${ANSWER_QUALITY_TELEGRAM_OPTIMIZED_PERCENTAGE:-100}
      DATABASE_URL: ${COMPOSE_DATABASE_URL:-}
      NODE_ENV: production
      KNOWLEDGE_AUTO_REFRESH_ENABLED: ${KNOWLEDGE_AUTO_REFRESH_ENABLED:-false}
      KNOWLEDGE_AUTO_REFRESH_INCREMENTAL_DAILY_AT: ${KNOWLEDGE_AUTO_REFRESH_INCREMENTAL_DAILY_AT:-08:00}
      KNOWLEDGE_AUTO_REFRESH_RECEIPT_FILE: ${KNOWLEDGE_AUTO_REFRESH_RECEIPT_FILE:-.rag/knowledge-refresh/latest.json}
      KNOWLEDGE_AUTO_REFRESH_STALE_AFTER_MINUTES: ${KNOWLEDGE_AUTO_REFRESH_STALE_AFTER_MINUTES:-1560}
      KNOWLEDGE_AUTO_REFRESH_TIME_ZONE: ${KNOWLEDGE_AUTO_REFRESH_TIME_ZONE:-Asia/Shanghai}
      OPENAI_API_KEY: ${OPENAI_API_KEY:?Set OPENAI_API_KEY in .env}
      OPENAI_BASE_URL: ${COMPOSE_OPENAI_BASE_URL:-${OPENAI_BASE_URL}}
      OPENAI_MODEL: ${OPENAI_MODEL:?Set OPENAI_MODEL in .env}
      ONCHAIN_ALLOW_INSECURE_LOCALHOST: ${ONCHAIN_ALLOW_INSECURE_LOCALHOST:-false}
      ONCHAIN_RPC_CONFIG_JSON: ${ONCHAIN_RPC_CONFIG_JSON:-}
      XXYY_SMALL_POOL_MAX_LIQUIDITY_USD: ${XXYY_SMALL_POOL_MAX_LIQUIDITY_USD:-10000}
      XXYY_SMALL_POOL_MAX_RELATIVE_LIQUIDITY_PPM: ${XXYY_SMALL_POOL_MAX_RELATIVE_LIQUIDITY_PPM:-100000}
      XXYY_CANONICAL_POOL_CONFIG_JSON: ${XXYY_CANONICAL_POOL_CONFIG_JSON:-}
      POSTGRES_DB: ${POSTGRES_DB:?Set POSTGRES_DB in .env}
      POSTGRES_HOST: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:?Set POSTGRES_USER in .env}
      HTTPS_PROXY: ${TELEGRAM_HTTPS_PROXY:-}
      HTTP_PROXY: ${TELEGRAM_HTTP_PROXY:-}
      NODE_USE_ENV_PROXY: ${TELEGRAM_NODE_USE_ENV_PROXY:-}
      NO_PROXY: ${TELEGRAM_NO_PROXY:-postgres,localhost,127.0.0.1}
      TELEGRAM_GROUP_RESPONSES_ENABLED: ${TELEGRAM_GROUP_RESPONSES_ENABLED:-false}
      TELEGRAM_GROUP_MESSAGE_RETENTION_DAYS: ${TELEGRAM_GROUP_MESSAGE_RETENTION_DAYS:-30}
      TELEGRAM_API_BASE_URL: ${TELEGRAM_API_BASE_URL:-https://api.telegram.org}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:?Set TELEGRAM_BOT_TOKEN in .env}
    depends_on:
      knowledge-init:
        condition: service_completed_successfully
    volumes:
      - './.rag/knowledge-refresh:/app/.rag/knowledge-refresh:ro'

  telegram-curation-worker:
    <<: *app-base
    command: ['pnpm', 'run', 'telegram:curation:worker']
    restart: unless-stopped
    environment:
      DATABASE_URL: ${COMPOSE_DATABASE_URL:-}
      NODE_ENV: production
      OPENAI_API_KEY: ${OPENAI_API_KEY:?Set OPENAI_API_KEY in .env}
      OPENAI_BASE_URL: ${COMPOSE_OPENAI_BASE_URL:-${OPENAI_BASE_URL}}
      OPENAI_MODEL: ${OPENAI_MODEL:?Set OPENAI_MODEL in .env}
      POSTGRES_DB: ${POSTGRES_DB:?Set POSTGRES_DB in .env}
      POSTGRES_HOST: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:?Set POSTGRES_USER in .env}
      HTTPS_PROXY: ${TELEGRAM_HTTPS_PROXY:-}
      HTTP_PROXY: ${TELEGRAM_HTTP_PROXY:-}
      NODE_USE_ENV_PROXY: ${TELEGRAM_NODE_USE_ENV_PROXY:-}
      NO_PROXY: ${TELEGRAM_NO_PROXY:-postgres,localhost,127.0.0.1}
      TELEGRAM_API_BASE_URL: ${TELEGRAM_API_BASE_URL:-https://api.telegram.org}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:?Set TELEGRAM_BOT_TOKEN in .env}
      TELEGRAM_CURATION_DEBOUNCE_SECONDS: ${TELEGRAM_CURATION_DEBOUNCE_SECONDS:-30}
      TELEGRAM_CURATION_WORKER_POLL_MS: ${TELEGRAM_CURATION_WORKER_POLL_MS:-1000}
      TELEGRAM_GROUP_MESSAGE_RETENTION_DAYS: ${TELEGRAM_GROUP_MESSAGE_RETENTION_DAYS:-30}
    depends_on:
      knowledge-init:
        condition: service_completed_successfully

  quality-worker:
    <<: *app-base
    command: ['pnpm', 'rag:quality:evaluation:worker']
    restart: unless-stopped
    depends_on:
      knowledge-init:
        condition: service_completed_successfully

volumes:
  xxyy_pgdata:
