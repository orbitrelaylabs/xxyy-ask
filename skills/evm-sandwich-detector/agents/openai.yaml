interface:
  display_name: 'EVM Sandwich Detector'
  short_description: 'Assess Sandwich evidence for an EVM swap'
  default_prompt: 'Use $evm-sandwich-detector to assess whether this EVM swap shows Sandwich evidence.'
dependencies:
  tools:
    - type: 'mcp'
      value: 'onchain-analysis'
      description: 'Read-only EVM transaction and Sandwich analysis'
      transport: 'stdio'
policy:
  allow_implicit_invocation: false
