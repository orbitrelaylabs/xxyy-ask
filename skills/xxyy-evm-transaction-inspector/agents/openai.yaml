interface:
  display_name: 'XXYY EVM Transaction Inspector'
  short_description: 'Explain public EVM transactions with evidence'
  default_prompt: 'Use $xxyy-evm-transaction-inspector to explain this public EVM transaction with evidence.'
dependencies:
  tools:
    - type: 'mcp'
      value: 'xxyy-chain-analysis'
      description: 'Governed read-only public EVM transaction analysis'
      transport: 'stdio'
policy:
  allow_implicit_invocation: false
